name: Deploy Reused

on:
  workflow_call:

jobs:
  check:
    runs-on: ubuntu-latest
    env:
      TEST: ${{ secrets.my-test-secret }}
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "test env is ${{ env.TEST }}"

